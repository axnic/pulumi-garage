{
  "name": "garage",
  "displayName": "pulumi-garage",
  "description": "A Pulumi provider to manage Garage S3-compatible object storage.",
  "keywords": [
    "garage",
    "s3",
    "object-storage",
    "self-hosted"
  ],
  "homepage": "https://github.com/axnic/pulumi-garage",
  "license": "Apache-2.0",
  "repository": "https://github.com/axnic/pulumi-garage",
  "pluginDownloadURL": "github://api.github.com/axnic/pulumi-garage",
  "publisher": "axnic",
  "namespace": "axnic",
  "language": {
    "csharp": {
      "respectSchemaVersion": true,
      "rootNamespace": "Axnic.Pulumi"
    },
    "go": {
      "generateResourceContainerTypes": true,
      "importBasePath": "github.com/axnic/pulumi-garage/sdk/go/pulumi-garage",
      "respectSchemaVersion": true
    },
    "nodejs": {
      "packageName": "@axnic/pulumi-garage",
      "respectSchemaVersion": true
    },
    "python": {
      "packageName": "pulumi_garage",
      "pyproject": {
        "enabled": true
      },
      "respectSchemaVersion": true
    }
  },
  "config": {
    "variables": {
      "accessKeyId": {
        "type": "string",
        "description": "An access key ID authorized against the Garage S3 API. Only required to manage a Bucket's lifecycleRules. Falls back to the GARAGE_S3_ACCESS_KEY_ID environment variable if not set."
      },
      "adminToken": {
        "type": "string",
        "description": "A bearer token authorized against the Garage Admin API. Falls back to the GARAGE_ADMIN_TOKEN environment variable if not set.",
        "secret": true
      },
      "endpoint": {
        "type": "string",
        "description": "The base URL of the Garage Admin API, e.g. \"http://localhost:3903\". Falls back to the GARAGE_ADMIN_ENDPOINT environment variable if not set."
      },
      "s3Endpoint": {
        "type": "string",
        "description": "The base URL of the Garage S3 API, e.g. \"http://localhost:3900\". Only required to manage a Bucket's lifecycleRules. Falls back to the GARAGE_S3_ENDPOINT environment variable if not set."
      },
      "s3Region": {
        "type": "string",
        "description": "The S3 region to sign requests for. Falls back to the GARAGE_S3_REGION environment variable, then to \"garage\" (Garage's own default) if neither is set."
      },
      "secretAccessKey": {
        "type": "string",
        "description": "The secret access key paired with accessKeyId. Only required to manage a Bucket's lifecycleRules. Falls back to the GARAGE_S3_SECRET_ACCESS_KEY environment variable if not set.",
        "secret": true
      }
    }
  },
  "types": {
    "garage:index:LifecycleRuleArgs": {
      "properties": {
        "abortIncompleteMultipartUploadDays": {
          "type": "integer",
          "description": "Abort incomplete multipart uploads this many days after they were initiated. Omit to not clean these up automatically."
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether this rule is active."
        },
        "expirationDays": {
          "type": "integer",
          "description": "Delete objects this many days after creation. Omit to not expire objects by age."
        },
        "id": {
          "type": "string",
          "description": "A unique identifier for this rule within the bucket."
        },
        "prefix": {
          "type": "string",
          "description": "Only objects whose key starts with this prefix are affected. Omit to match every object in the bucket."
        }
      },
      "type": "object",
      "required": [
        "id",
        "enabled"
      ]
    },
    "garage:index:PermissionsArgs": {
      "properties": {
        "owner": {
          "type": "boolean",
          "description": "Whether the key has owner rights on the bucket (manage bucket-level settings such as its website configuration or quotas via the S3 API)."
        },
        "read": {
          "type": "boolean",
          "description": "Whether the key can read objects (GetObject, ListObjects, ...) from the bucket."
        },
        "write": {
          "type": "boolean",
          "description": "Whether the key can write objects (PutObject, DeleteObject, ...) to the bucket."
        }
      },
      "type": "object"
    },
    "garage:index:QuotasArgs": {
      "properties": {
        "maxObjects": {
          "type": "integer",
          "description": "The maximum number of objects the bucket may hold. Unset means no limit."
        },
        "maxSize": {
          "type": "integer",
          "description": "The maximum total size, in bytes, the bucket may hold. Unset means no limit."
        }
      },
      "type": "object"
    },
    "garage:index:WebsiteArgs": {
      "properties": {
        "errorDocument": {
          "type": "string",
          "description": "The document served for requests that don't match an existing object. Defaults to Garage's built-in error page if unset."
        },
        "indexDocument": {
          "type": "string",
          "description": "The document served for requests to the bucket root or any \"directory\"."
        }
      },
      "type": "object",
      "required": [
        "indexDocument"
      ]
    }
  },
  "provider": {
    "properties": {
      "accessKeyId": {
        "type": "string",
        "description": "An access key ID authorized against the Garage S3 API. Only required to manage a Bucket's lifecycleRules. Falls back to the GARAGE_S3_ACCESS_KEY_ID environment variable if not set."
      },
      "adminToken": {
        "type": "string",
        "description": "A bearer token authorized against the Garage Admin API. Falls back to the GARAGE_ADMIN_TOKEN environment variable if not set.",
        "secret": true
      },
      "endpoint": {
        "type": "string",
        "description": "The base URL of the Garage Admin API, e.g. \"http://localhost:3903\". Falls back to the GARAGE_ADMIN_ENDPOINT environment variable if not set."
      },
      "s3Endpoint": {
        "type": "string",
        "description": "The base URL of the Garage S3 API, e.g. \"http://localhost:3900\". Only required to manage a Bucket's lifecycleRules. Falls back to the GARAGE_S3_ENDPOINT environment variable if not set."
      },
      "s3Region": {
        "type": "string",
        "description": "The S3 region to sign requests for. Falls back to the GARAGE_S3_REGION environment variable, then to \"garage\" (Garage's own default) if neither is set."
      },
      "secretAccessKey": {
        "type": "string",
        "description": "The secret access key paired with accessKeyId. Only required to manage a Bucket's lifecycleRules. Falls back to the GARAGE_S3_SECRET_ACCESS_KEY environment variable if not set.",
        "secret": true
      }
    },
    "inputProperties": {
      "accessKeyId": {
        "type": "string",
        "description": "An access key ID authorized against the Garage S3 API. Only required to manage a Bucket's lifecycleRules. Falls back to the GARAGE_S3_ACCESS_KEY_ID environment variable if not set."
      },
      "adminToken": {
        "type": "string",
        "description": "A bearer token authorized against the Garage Admin API. Falls back to the GARAGE_ADMIN_TOKEN environment variable if not set.",
        "secret": true
      },
      "endpoint": {
        "type": "string",
        "description": "The base URL of the Garage Admin API, e.g. \"http://localhost:3903\". Falls back to the GARAGE_ADMIN_ENDPOINT environment variable if not set."
      },
      "s3Endpoint": {
        "type": "string",
        "description": "The base URL of the Garage S3 API, e.g. \"http://localhost:3900\". Only required to manage a Bucket's lifecycleRules. Falls back to the GARAGE_S3_ENDPOINT environment variable if not set."
      },
      "s3Region": {
        "type": "string",
        "description": "The S3 region to sign requests for. Falls back to the GARAGE_S3_REGION environment variable, then to \"garage\" (Garage's own default) if neither is set."
      },
      "secretAccessKey": {
        "type": "string",
        "description": "The secret access key paired with accessKeyId. Only required to manage a Bucket's lifecycleRules. Falls back to the GARAGE_S3_SECRET_ACCESS_KEY environment variable if not set.",
        "secret": true
      }
    }
  },
  "resources": {
    "garage:index:Bucket": {
      "properties": {
        "bytes": {
          "type": "integer",
          "description": "The total size, in bytes, of all objects currently stored in the bucket."
        },
        "createdAt": {
          "type": "string",
          "description": "The RFC 3339 timestamp at which the bucket was created."
        },
        "globalAlias": {
          "type": "string",
          "description": "The bucket's human-readable global alias, e.g. \"my-app-data\". Buckets can be created without one and addressed only by ID, but an alias is required to use the bucket over the S3 API with most clients. Only a single global alias is supported; local (per-key) aliases and multiple global aliases are not managed by this provider."
        },
        "lifecycleRules": {
          "type": "array",
          "items": {
            "$ref": "#/types/garage:index:LifecycleRuleArgs"
          },
          "description": "S3 lifecycle rules for the bucket (object expiration, incomplete-multipart-upload cleanup). Requires the provider's S3 API config (s3Endpoint/accessKeyId/secretAccessKey) to be set, and globalAlias to be set on this bucket, since lifecycle rules are managed over the S3 API, which addresses buckets by name rather than by internal ID."
        },
        "objects": {
          "type": "integer",
          "description": "The number of objects currently stored in the bucket."
        },
        "quotas": {
          "$ref": "#/types/garage:index:QuotasArgs",
          "description": "Storage quotas for the bucket. Omit either field, or the whole block, to leave that limit unset."
        },
        "website": {
          "$ref": "#/types/garage:index:WebsiteArgs",
          "description": "Static-website hosting configuration for the bucket. Omit to leave website hosting disabled."
        }
      },
      "required": [
        "createdAt",
        "objects",
        "bytes"
      ],
      "inputProperties": {
        "globalAlias": {
          "type": "string",
          "description": "The bucket's human-readable global alias, e.g. \"my-app-data\". Buckets can be created without one and addressed only by ID, but an alias is required to use the bucket over the S3 API with most clients. Only a single global alias is supported; local (per-key) aliases and multiple global aliases are not managed by this provider."
        },
        "lifecycleRules": {
          "type": "array",
          "items": {
            "$ref": "#/types/garage:index:LifecycleRuleArgs"
          },
          "description": "S3 lifecycle rules for the bucket (object expiration, incomplete-multipart-upload cleanup). Requires the provider's S3 API config (s3Endpoint/accessKeyId/secretAccessKey) to be set, and globalAlias to be set on this bucket, since lifecycle rules are managed over the S3 API, which addresses buckets by name rather than by internal ID."
        },
        "quotas": {
          "$ref": "#/types/garage:index:QuotasArgs",
          "description": "Storage quotas for the bucket. Omit either field, or the whole block, to leave that limit unset."
        },
        "website": {
          "$ref": "#/types/garage:index:WebsiteArgs",
          "description": "Static-website hosting configuration for the bucket. Omit to leave website hosting disabled."
        }
      }
    },
    "garage:index:BucketKeyPermission": {
      "properties": {
        "accessKeyId": {
          "type": "string",
          "description": "The access key ID of the Key to grant permissions to."
        },
        "bucketId": {
          "type": "string",
          "description": "The ID of the Bucket to grant permissions on."
        },
        "permissions": {
          "$ref": "#/types/garage:index:PermissionsArgs",
          "description": "The read/write/owner permissions to grant."
        }
      },
      "required": [
        "bucketId",
        "accessKeyId",
        "permissions"
      ],
      "inputProperties": {
        "accessKeyId": {
          "type": "string",
          "description": "The access key ID of the Key to grant permissions to."
        },
        "bucketId": {
          "type": "string",
          "description": "The ID of the Bucket to grant permissions on."
        },
        "permissions": {
          "$ref": "#/types/garage:index:PermissionsArgs",
          "description": "The read/write/owner permissions to grant."
        }
      },
      "requiredInputs": [
        "bucketId",
        "accessKeyId",
        "permissions"
      ]
    },
    "garage:index:Key": {
      "properties": {
        "accessKeyId": {
          "type": "string",
          "description": "The S3 access key ID, e.g. the value of AWS_ACCESS_KEY_ID."
        },
        "createdAt": {
          "type": "string",
          "description": "The RFC 3339 timestamp at which the key was created."
        },
        "name": {
          "type": "string",
          "description": "A human-readable label for the key. If unset, Garage assigns a default name."
        },
        "secretAccessKey": {
          "type": "string",
          "description": "The S3 secret access key, e.g. the value of AWS_SECRET_ACCESS_KEY. Only ever readable at creation time - Garage's Admin API does not return it again afterwards, so it is captured once here and carried forward in state.",
          "secret": true
        }
      },
      "required": [
        "accessKeyId",
        "secretAccessKey",
        "createdAt"
      ],
      "inputProperties": {
        "name": {
          "type": "string",
          "description": "A human-readable label for the key. If unset, Garage assigns a default name."
        }
      }
    }
  }
}
