# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
#
# E2E against Garage __VER__ - auto-generated by sync-e2e-matrix workflow.

name: E2E (Garage __VER__)

on:
  merge_group: {}
  pull_request: {}
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  e2e:
    permissions:
      contents: read
    uses: ./.github/workflows/_reusable-e2e.yaml
    with:
      garage-version: __VER__
